scan input script into tokens
parse tokens into AST objects
condense ids into object references, append buses to split outputs
type inference of storage types (can avoid somehow?)
from process endpoints backwards, set up process runners, storages, and buses
execute processes
